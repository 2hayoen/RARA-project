<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rara.app.model.mapper.MemberMapper">


    <resultMap id="MemberMap" type="com.rara.app.dto.MemberDTO">
        <result column="ID" property="id"/>
        <result column="TITLE" property="title"/>
        <result column="DESCRIPTION" property="description"/>
        <result column="PUBLISHED" property="published"/>
    </resultMap>

    <!-- Insert Member -->
    <insert id="insertMember">
        {call tutorials_pkg.create_tutorial(#{title, jdbcType=VARCHAR}, #{description, jdbcType=VARCHAR}, #{published, jdbcType=INTEGER})}
    </insert>

    <!-- Delete Member -->
    <delete id="deleteMember">
        {call tutorials_pkg.delete_tutorial(#{id, jdbcType=INTEGER})}
    </delete>

    <!-- Update Member -->
    <update id="updateMember">
        {call tutorials_pkg.update_tutorial(#{id, jdbcType=INTEGER}, #{title, jdbcType=VARCHAR}, #{description, jdbcType=VARCHAR}, #{published, jdbcType=INTEGER})}
    </update>

    <!-- Select Member -->
    <select id="selectMember" resultType="com.rara.app.dto.MemberDTO">
        select * from tutorials
    </select>

    <select id="freadMember" resultMap="MemberMap" statementType="CALLABLE">
        { call #{cursor, mode=OUT, jdbcType=CURSOR, javaType=java.sql.ResultSet, resultMap=MyMap} :=
        tutorials_pkg.read_tutorial() }
    </select>


</mapper>